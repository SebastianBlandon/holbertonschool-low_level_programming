#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - the function print the digit nums
 * Return: 0
 */
int main(void)
{
	int n;

	for (n = 0 ; n < 10 ; n++)
		printf("%i", n);
	printf("\n");
	return (0);
}
